# Js

## Purpose

Client-side behavior shared by the portfolio pages.

## Contents

Site initialization, theme persistence, blog rendering, scroll-reveal behavior, and content-card navigation modules.

## Responsibilities

Browser-side interaction logic belongs here; content records belong in `assets/data/`.

## Important Notes

The modules are loaded directly by static HTML pages and must remain browser-compatible without a build step.
