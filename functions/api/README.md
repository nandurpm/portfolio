# Api

## Purpose

HTTP API route namespace for deployed server-side handlers.

## Contents

Subdirectories map URL paths to backend endpoints.

## Responsibilities

Add API handlers under the route that matches their public URL.

## Important Notes

Keep browser-only UI logic in `admin/` or `assets/js/`, not in this directory.
