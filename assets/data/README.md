# Data

## Purpose

JSON content indexes consumed by the portfolio UI.

## Contents

`works.json`, `blog.json`, and `media.json` records used to render projects, articles, and media.

## Responsibilities

Store content metadata and references here; rendering logic belongs in `assets/js/`.

## Important Notes

Keep field names and referenced asset paths compatible with the client-side loaders.
